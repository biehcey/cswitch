# Changelog

All notable changes to this project are documented here.

## [Unreleased]

- Initial packaging: `npm i -g cswitch`, `init` → `add` → `shell-init` setup flow, `bind`/`unbind`,
  `status`, and the launcher core.
