{
  "name": "@biehcey/cswitch",
  "version": "0.1.5",
  "description": "Run multiple Claude Code accounts on one machine",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/biehcey/cswitch.git"
  },
  "type": "module",
  "bin": {
    "cswitch": "dist/cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "cross-spawn": "^7.0.6"
  },
  "devDependencies": {
    "@types/cross-spawn": "^6.0.6",
    "@types/node": "^24.0.0",
    "tsx": "^4.23.13",
    "typescript": "^5.7.0"
  }
}
